@model IEnumerable<Owner>

<div class="row">
    <a class="btn btn-success mb-3" asp-controller="Owner" asp-action="Create">Create</a>
    <div class="col-12">
        <table class="table table-striped">
            <thead>
                <th>ID</th>
                <th>NAME</th>
                <th>PHONE</th>
                <th class="text-end">Actions</th>
            </thead>
            <tbody>
            @foreach (var o in Model)
            {
                <tr>
                    <td>@o.Id</td>
                    <td>@o.Name</td>
                    <td>@o.Phone</td>
                    <td class="text-end">
                        <a class="btn btn-outline-warning" asp-controller="Owner" asp-action="Update" asp-route-id="@o.Id">Edit</a>
                        <form asp-controller="Owner" asp-action="Delete" asp-route-id="@o.Id" method="post" style="display:inline">
                            <button type="submit" class="btn btn-outline-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
