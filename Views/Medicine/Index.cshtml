@model IEnumerable<Medicine>

<div class="row">
    <a class="btn btn-success mb-3" asp-controller="Medicine" asp-action="Create">Create</a>
    <div class="col-12">
        <table class="table table-striped">
            <thead>
                <th>ID</th>
                <th>NAME</th>
                <th>STOCK</th>
                <th class="text-end">Actions</th>
            </thead>
            <tbody>
            @foreach (var m in Model)
            {
                <tr>
                    <td>@m.Id</td>
                    <td>@m.Name</td>
                    <td>@m.Stock</td>
                    <td class="text-end">
                        <a class="btn btn-outline-warning" asp-controller="Medicine" asp-action="Update" asp-route-id="@m.Id">Edit</a>
                        <form asp-controller="Medicine" asp-action="Delete" asp-route-id="@m.Id" method="post" style="display:inline">
                            <button type="submit" class="btn btn-outline-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
